version: "3.9"
services:
  nanobot-website:
    image: nanobot-website
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nanobot-website
    environment:
      - NODE_ENV=development
    ports:
      - "3000:3000"
